@{
    Layout = "~/Views/Shared/_ThemeLayout.cshtml";
    ViewData["Title"] = "Import Nhân Viên Từ Excel";
}

<div class="container py-5">
    <div class="card shadow border-0">
        <div class="card-header bg-success text-white py-4">
            <h2 class="mb-0 fw-bold"><i class="fa fa-file-excel-o me-2"></i>Import Nhân Viên</h2>
        </div>
        <div class="card-body p-4">
            <ol class="text-muted small">
                <li>Tải file mẫu Excel</li>
                <li>Nhập thông tin tại Sheet <code>NhanVienImport</code></li>
                <li>Tạo folder <code>wwwroot/avatars</code> và đặt tên file ảnh theo cú pháp <code>email-sdt.jpg</code></li>
                <li>Sau đó upload file Excel để xem trước và lưu hàng loạt</li>
            </ol>

            <a href="/EmployeeImport/DownloadTemplate" class="btn btn-outline-success mb-4">
                <i class="fa fa-download me-2"></i>Tải File Mẫu Excel
            </a>

            <form method="post" enctype="multipart/form-data" asp-action="UploadExcel">
                <div class="mb-3">
                    <input type="file" name="file" accept=".xlsx" class="form-control" required />
                </div>
                <button type="submit" class="btn btn-primary px-4">
                    <i class="fa fa-upload me-2"></i>Upload & Xem Trước
                </button>
            </form>
        </div>
    </div>
</div>
